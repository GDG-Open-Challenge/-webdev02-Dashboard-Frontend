function moveCard(a, b) { if (AppState.cards[a]) { var c = AppState.cards[a].columnId; if (c === b) return; AppState.cards[a].columnId = b, reorderCards(c), reorderCards(b), addActivity("Moved card to " + b), autoSave(), renderBoard() } }